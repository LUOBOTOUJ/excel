<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.vitality.material.mapper.CutMapper">
    <update id="updateStatus" parameterType="com.vitality.material.entity.Cut">
        update tbl_inf_exp_cut set status = 'P' where exp_cut_trans_id = #{expCutTransId}
    </update>

</mapper>
